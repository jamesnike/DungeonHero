---
description: "Reducer 内部 enqueue 的 follow-up action 必须加进 pipeline.ts 的 isInputContinuation 白名单，否则会被 playerInput 相位卡死在队列里"
alwaysApply: true
---

# Pipeline `isInputContinuation` —— 内部 follow-up action 必须加白名单

## 一次真实事故

User 报：「Ogre 落到 active row 时不触发入场效果（`auto-engage` 全军激怒），
但 Ogre 死亡时反而触发了入场技能。」

根因：`MONSTER_ENTERED_ROW` 没被列进 `client/src/game-core/pipeline.ts` 的
`isInputContinuation` 白名单。

具体死亡链：

1. 瀑流把 Ogre 落到行上 → `APPLY_WATERFALL_DROP` reducer enqueue 一条
   `MONSTER_ENTERED_ROW`。
2. drain 循环进到下一步，发现 `current.phase === 'playerInput'`（瀑流动画
   结束后的常态相位）。
3. `isInputContinuation('MONSTER_ENTERED_ROW') === false` → pipeline **暂停**，
   `MONSTER_ENTERED_ROW` 留在 `state.actionQueue` 里。
4. 玩家下一次 dispatch（拖武器去打 Ogre = `INITIATE_WEAPON_ATTACK`）触发
   新一轮 drain → 队列里残留的 `MONSTER_ENTERED_ROW` 这才被处理 →
   `enter:auto-engage` float 和 `combat:autoEngage` side effect 这时才发出。
5. 视觉上：玩家看到「入场·全军激怒」float 出现在 Ogre 即将被打死的瞬间 →
   误以为「入场技能在死亡时触发」。

为什么旧测试没抓到：`_tmp/ogre-engine.test.ts` 用的是
`createInitialGameState()`，`phase` 默认 `'idle'`（不在 `INPUT_PHASES` 里），
所以 gating 不生效，测试通过。**真实游戏里 phase 一直是 `'playerInput'`。**

## 强制规则

### 1. Reducer enqueue 新 action 类型时，先问自己

> "这个 action 是不是从 reducer 内部 enqueue 的『系统层 follow-up』？
> （不是玩家点出来的、不是 UI dispatch 的、reducer 内部 push 进 enqueuedActions 的）"

如果是 ✅，**必须**在 `client/src/game-core/pipeline.ts` 的 `isInputContinuation` switch 里加 `case 'YOUR_ACTION':`。

否则只要这个 action 在 `phase='playerInput'`（或任何 `INPUT_PHASES` 相位）下被 enqueue，
就会被 drain 循环卡住，留在队列里直到下一次 dispatch 才被动处理 ——
而那时游戏状态可能已经变了，效果要么打到错的目标，要么时机违和。

### 2. INPUT_PHASES 列表（截至本规则编写时）

```
'awaitingBlock' | 'playerInput' | 'awaitingTarget' | 'awaitingDice' |
'awaitingEventChoice' | 'awaitingShopAction' | 'awaitingRewardChoice' |
'awaitingPotionTarget' | 'awaitingMagicTarget' | 'awaitingDeathWard' |
'awaitingEquipmentPrompt' | 'awaitingDiscoverChoice' |
'awaitingUpgradeChoice' | 'awaitingDeleteChoice'
```

**`'playerInput'` 是 99% 时间游戏所处的相位。** 任何 follow-up action
都要预设它会在 `'playerInput'` 下被 enqueue。

### 3. 测试必须用 `phase: 'playerInput'`

测 enter / waterfall / 内部 follow-up action 链时，**绝对不要**用
`createInitialGameState()` 的默认 `phase: 'idle'`。要显式：

```ts
const state = makeState({
  phase: 'playerInput',
  // ...其它 fixture
});
```

否则 `INPUT_PHASES` 检查永远走不到，gating bug 测不出来。

参考实现：`client/src/game-core/__tests__/ogre-enter-effect-on-waterfall-drop.test.ts`。

### 4. 区分两种 action

| 类型 | 是否加白名单 |
|---|---|
| Reducer 内部 enqueue 的系统 follow-up（damage、heal、enter、check 类） | ✅ 必须加 |
| 真正需要新玩家决策的 action（开新 modal、要求选目标） | ❌ 不要加 —— pause 在这里是对的 |
| UI 直接 `engine.dispatch(...)` 的顶层 action | ❓ `_processAction` 不走这个 gate（reduce 直接执行），但它 enqueue 的 follow-up 走 |

## 已知白名单缺口的曾经 bug 模式

| Action | 影响 | 修复 commit |
|---|---|---|
| `MONSTER_ENTERED_ROW` | Ogre `auto-engage` / 精英 Ogre `ogreEnterDiscard` 入场效果延迟到下一次 dispatch 才触发 | 本规则编写时已修 |
| `UPDATE_GAME_LOG`（震慑领域 stun-domain） | RESOLVE_DICE 末尾 enqueue `[UPDATE_GAME_LOG, FINALIZE_MAGIC_CARD]`，drain 卡在 UPDATE_GAME_LOG → 卡牌进 limbo（手牌没了但没进坟场） | pipeline.ts 注释 |
| `TRIGGER_ON_ENTER_HAND` | 上手关键字（生长之刃 amplify 等）卡在队列，卡永远拿不到 buff | pipeline.ts 注释 |

新加 case 时**照旧的注释格式写一段 bug 历史**，让下次的人能秒懂为什么这条 case 在这。

## 自检

每次准备在 reducer 里写 `enqueuedActions.push({ type: 'XXX', ... })` 之前，停下来问自己：

> 1. `'XXX'` 在 `pipeline.ts` 的 `isInputContinuation` switch 里有 case 吗？
> 2. 如果没有，它会不会在 `phase === 'playerInput'` 下被 enqueue？（99% 情况是会）
> 3. 我的测试 fixture 是不是用了 `phase: 'playerInput'`，而不是默认的 `'idle'`？

任意一条「没」或「不确定」，回头补 case + 改测试 fixture 再继续。

## 跟其它规则的关系

| 关注点 | 这条规则 | 其它规则 |
|---|---|---|
| 内部 follow-up action 在 INPUT 相位下能不能 drain | ✅ 这条 | ❌ |
| 多步 reducer 逻辑用 enqueuedActions 而不是 hook 顺序 dispatch | ❌ | `reducer-patterns` |
| 需要玩家输入的 request → resolve 流程 | ❌ | `interactive-continuation-flows` |
| 测试要用真实 dispatch 链而不是孤立 reduce 调用 | ❌ | `testing` |
