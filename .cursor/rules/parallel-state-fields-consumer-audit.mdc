---
description: 2选1 / 多选 1 效果用 N 个并行字段时，必须审计每个消费方读对了字段；并验证字段生命周期（永久 vs 每回合重置）跨 resolver/consumer/reset/persistence 一致
alwaysApply: true
---

# 多选效果的并行字段：消费方必须读对字段 + 生命周期一致

## 背景：一次真实事故（潮涌铸甲）

`潮涌铸甲` 是 2 选 1 卡：选 A 给永恒护符·瀑流铸剑（攻击时该栏临时攻击 +2），
选 B 给永恒护符·格挡铸甲（格挡时该栏临时护甲 +2）。两个效果用**两个独立字段**追踪层数：

- `bulwarkPassiveActive` = 瀑流铸剑（攻击）层数
- `bulwarkTempArmorStacks` = 格挡铸甲（格挡）层数

User 报"我只选了瀑流铸剑（攻击），结果格挡时也加了临时护甲"。根因是两个 bug：

1. **消费方读错字段**：`combat.ts:3392` 格挡分支判定用 `bulwarkPassiveActive`（攻击层数）
   而不是 `bulwarkTempArmorStacks`（格挡层数）。攻击层数 > 0 时，格挡也错触发"格挡铸甲"。

2. **字段生命周期不一致**：`bulwarkTempArmorStacks` 在 resolver 是**永久层数**（累加 +1）、
   在 HeroInfoContainer/persistence 也按永久处理，但 `waterfall.ts:206` 每次瀑流把它清零。
   修了 #1 之后这个 bug 才会暴露：玩家选了"格挡铸甲"，第一次瀑流后护符就"不见了"。

两个 bug 互相掩盖：bug #1 错读字段让"格挡效果"在攻击层数还在时还能触发，掩盖了 bug #2 的清零问题。

## 强制规则

### 1. 多个并行字段的命名 + 注释必须能区分

新加 2 选 1 / 多选 1 卡，用独立字段追踪每个分支的状态时：

- 字段名要能让人一眼看出**对应哪个分支**（避免 `xxxActive` / `xxxStacks` 这种通用后缀让人猜）。
- 在 `GameState`（`types.ts`）的字段定义处加 JSDoc，写清"这是 X 选 Y 的 Z 分支层数，
  在 `<effectId>` 分支被选时累加，被 `<consumerFile:line>` 消费"。

### 2. 改 / 加任何"该状态字段触发的"消费方代码前，必须验证读对字段

每次准备写：

```typescript
if ((state.<someBulwarkLikeField> ?? 0) > 0) { /* ... */ }
```

之前停下来：

1. **`Grep` 字段名**找出所有写入位置（resolver），确认"什么 effect / 什么分支"会把它累加。
2. **跟当前代码块的语义对一遍**：你这段代码是在"攻击"还是"格挡"还是"瀑流"路径？
   读的字段是不是**对应该路径的那个分支**？
3. 如果 2 选 1 卡有两个并行字段（A 字段 / B 字段），同一个文件里两段消费代码很容易复制粘贴
   出错。**两段都要逐字对一遍**，不要假设"上面那段写对了下面也对"。

### 3. 字段生命周期跨四个位置必须一致

任何 `GameState` 字段的"语义生命周期"必须在以下四个位置**完全一致**：

| 位置 | 语义来源 |
|---|---|
| **写入**（resolver / reducer） | `+= 1` = 永久累加；`= value` 一次性写 = 当下一次 |
| **重置**（`waterfallResetsPure` / `turnResetsPure` / 战斗结束） | 出现在重置列表 = 短生命周期（每回合 / 每瀑流清零） |
| **持久化**（`persistence.ts` / `gameStorage.ts` `PersistedGameState`） | 出现在持久化字段 = 跨存档保留（必须是永久语义） |
| **显示**（HeroInfoContainer / 护符栏 / Modal） | 显示成"层数 / 累计" = 永久；显示成"本回合 X 次" = 短生命周期 |

**矛盾即 bug**。常见反模式（都是事故现场）：

- ❌ 写入是 `+=`（累加）+ 持久化保留 + **却被 `waterfallResetsPure` 清零**
  → 用户体感"护符第一次瀑流后失效"
- ❌ 写入是 `+=`（累加）+ 显示成"本回合"
  → 显示数字一直涨，让用户以为坏了
- ❌ 写入是一次性 `= value` + 没在任何地方重置
  → 老值一直残留，下一回合还在生效

### 4. 修一个 bug 时必须沿同一个字段做一圈生命周期审计

任何时候修改"某个字段被错读 / 被错写"的 bug，**不要只修那一处**：

1. `Grep` 这个字段名，列出所有出现位置（写入 / 重置 / 持久化 / 显示 / 消费）。
2. 对照上面那张表，确认四个位置的语义一致。
3. 如果发现第二个 bug（如本次的 `waterfall.ts` 清零），跟用户报告"修这个会暴露另一个问题"，
   一起修或确认范围。
4. **不要修 bug A 只看 bug A 的那一行**——同一个被搞混的字段往往藏着 bug B、bug C。

## 自检：写完任何涉及"多分支并行字段"或"修改生命周期相关代码"的改动，问自己

1. 我读的字段是不是**对应当前代码块的语义分支**？（攻击代码读攻击字段、格挡代码读格挡字段）
2. 这个字段在 resolver / reset / persistence / 显示 四个位置的生命周期描述能不能对上？
3. 同一文件里有没有"复制粘贴另一个分支"的兄弟代码块？两段都对过了吗？
4. 修的这个 bug 是不是只是冰山一角？同一个字段还有没有别的被搞错的地方？

任意一条"没"或"不确定"，**回头补功课再改**。

## 跟其它规则的关系

| 关注点 | 这条规则 | 其它规则 |
|---|---|---|
| 多分支并行字段被消费方读错 | ✅ 这条 | ❌ |
| 字段生命周期（永久 vs 每回合）跨多文件一致 | ✅ 这条 | ❌ |
| 同一 effectId 被多张卡共用，消费方未同步 | ❌ | `shared-effect-id-impact-check` |
| Amulet 字段必须按 N 层叠加 | ❌ | `amulet-stacking-design` |
| 同一 effectId 在旧 switch + schema 两处都要改 | ❌ | `shared-effect-id-impact-check` |

涉及多分支字段且与 amulet / 永恒护符 相关时，这几条规则**都要核对**。
