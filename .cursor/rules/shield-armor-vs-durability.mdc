---
description: 护盾的"护甲值 vs 耐久"正确模型 — single-counter armor + immediate refill on equip / bonus add
alwaysApply: true
---

# 护盾机制：armor 是血量，durability 是血层（single-counter armor model）

## 一次因为概念错乱差点改坏的事故

User 让我改 `守护圣盾` 的效果："完美格挡时 50% 不消耗护甲值"。
我的脑内模型是错的——以为护甲值（`value` / `armorMax`）是个**永远不变的常量**，只用来跟攻击力比大小决定"是否完美格挡"，每次格挡只扣 1 耐久。
基于这个错误模型，我反复跟 user 说"游戏现在没有'消耗护甲值'这个概念，你想引入新机制吗？"——
来回拉扯了 5、6 轮，user 几乎要放弃解释。
最后我去读 `client/src/game-core/rules/combat.ts` `reduceResolveBlock`，才发现 user 一直是对的，**我把游戏机制搞反了**。

## 正确模型（single-counter armor，自 2026 年护甲重构后）

> **`armor` 是这一层耐久内剩余的"护甲血量"（**单一统一计数器**）；`durability` 是"护甲血层数"。**

| 字段 | 角色 | 卡面位置 |
|---|---|---|
| `value` / `armorMax` | **基础**护甲值上限（每个新血层刷满到这个 + 永久 + 临时加成） | 卡牌正中大数字 |
| `armor` | 当前血层剩余护甲（**包含**永久+临时加成；`undefined` ⇒ "至 cap"） | —（运行时） |
| `durability` / `maxDurability` | 还有几个血层 | 卡牌角落 X/Y |
| `equipmentSlotBonuses[slotId].shield` | 该装备栏的永久护甲加成（state field，**不**写进 item） | —（运行时） |
| `slotTempArmor[slotId]` | 该装备栏的临时护甲加成（state field，**不**写进 item） | —（运行时） |

### 核心不变量

1. **统一计数器**：`armor` 字段就是当前可用护甲值，没有"基础 + 加成 - 累计已损"那种分池/分债算法。
2. **Cap = base + perm + temp**：`getSlotArmorCap(state, slotId) = baseArmorMax + slotPermShieldBonus + slotTempArmor`。
3. **`armor === undefined` ⇒ "fresh, at full cap"**。读取时走 `getSlotCurrentArmor`，看到 undefined 就返回 cap。
4. **Immediate refill on add**：装备入场 / 加永久护甲 / 加临时护甲 时，`armor` 立刻被刷到新 cap（**不是单纯增加 cap 而 armor 不动**）。这是玩家的核心直觉。
5. **Clamp on temp expire**：临时护甲过期时，cap 下降，`armor = min(armor, new cap)`。永久护甲减少时同理 clamp。
6. **Refill on layer break**：armor 打穿后，下一次读取走 `armor === undefined` 路径自动回满到当前 cap（包含永久+临时）。

每次格挡走的真实流程（简化）：

```
currentArmor = getSlotCurrentArmor(state, slotId)   // armor === undefined ⇒ cap = base+perm+temp
blocked      = min(attack, currentArmor)
newArmor     = max(0, currentArmor - attack)         // armor 被攻击啃掉
overflow     = max(0, attack - currentArmor)         // 溢出走 hero（非 fullBlock）

if newArmor == 0 && attack > 0:
  shieldArmorDepleted = true                         // 这一血层被打穿
  durability -= 1                                    // 扣一个血层
  // strip slotItem.armor → 下次读取自动回满到 cap（包含永久+临时）
else:
  slotItem.armor = newArmor                          // 写回扣减后的值

完美格挡（perfect block）= isFullBlockShield || overflow == 0
                       = "本次攻击没有溢出到英雄"
```

### 显示规则（GameCard.tsx）

护盾和怪物装备的护甲值显示**单一数字 Z**（不是 `X+Y`）：

```
Z = card.armor === undefined ? cap : Math.max(0, Math.min(card.armor, cap))
其中 cap = baseArmorMax + perm + temp
```

不要回退到旧的"基础 + 加成"双数字显示。

## 几个会被脑补错的边界

| 场景 | 错误直觉 | 真实行为 |
|---|---|---|
| 守护圣盾 base armor 3 / 耐久 2/2，怪打 2 | "完美格挡，耐久 -1，armor 不变 → 3 / 1/2" | armor: 3 → 1，耐久不变 → 显示 1 / 2/2 |
| 守护圣盾 base armor 3 / 耐久 2/2，怪打 3 | "完美格挡，耐久 -1，armor 不变 → 3 / 1/2" | armor 打穿，耐久 -1，armor 刷满 → 显示 3 / 1/2（结果数字一样，机制完全不同） |
| 守护圣盾 base armor 3 / 耐久 2/2，怪打 5 | "armor 吃 3，耐久 -1，hero 受 2" | 对的，但同时 armor 被打穿、armor 重置到 cap、英雄受 2 溢出 |
| 铁壁塔盾（fullBlock）base 5 / 耐久 1/1，怪打 8 | "fullBlock，盾完全挡 8，armor 不变" | armor: 5 → 0 (打穿)、耐久 -1 (盾损毁)、英雄 0 溢出。fullBlock 仅意味着溢出永远 = 0，不意味着 armor 不被扣 |
| 装一把 base armor 3 盾，槽有 perm +2 | "armor 显示 5（cap），扣到 0 才打穿" | ✅ 对：装备时立即 refill；armor=cap=5；扣到 0 触发 durability -1 |
| Base armor 3，perm +2 → +3（玩家用药）| "cap 涨到 6，armor 不动还是 5" | ❌ 错。`applySlotArmorBonusDelta` 把 armor +1 → 6（**immediate refill bonus**） |
| Base armor 3，temp +2 过期 | "cap 退到 3，armor 还是 5" | ❌ 错。Clamp：armor = min(5, 3) = 3 |

## 涉及"护盾耐久 / 护甲"逻辑修改时的强制流程

1. **先读 `client/src/game-core/equipment.ts`** 顶部的 single-counter armor model 注释 + 4 个 helper（`getSlotArmorCap` / `getSlotCurrentArmor` / `applySlotArmorBonusDelta` / `refillSlotArmorToCap`）。
2. **任何修改 `equipmentSlotBonuses[slotId].shield` 或 `slotTempArmor[slotId]` 的代码必须紧跟一个 `applySlotArmorBonusDelta(state, slotId, delta, patch)`**，否则永久/临时护甲变化时 armor 不跟着调整 → 玩家看到护盾"加了等于没加"或"过期了护甲不退"。
3. **任何让 `equipmentSlotN` item 改变（装备 / 替换 / swap）的代码 必要时调 `refillSlotArmorToCap(state, slotId, patch)`** —— 装备到新槽时让 armor 立即刷到 cap。
4. **不要给 item 写 `armorBonusDamaged` 字段** —— 该字段已经在重构中**完全删除**。`resetEquipmentForGraveyard` 只为兼容旧存档保留 `_armorBonusDamaged` destructure。
5. **monster 盾分支**和非 monster 盾分支共用同一套 single-counter 模型 —— 改怪物装备护盾时不要再去找"另一套对称实现"。
6. **修改后必跑** `client/src/game-core/__tests__/` 下所有 `*shield*` / `*armor*` 测试。

## 自检：跟 user 讨论护盾 / 怪物装备护甲前先问自己

> 1. 我是不是又把 armor 当成"不变的 stat"或"基础 vs 加成两池"了？这是 single-counter 模型，**只有一个 armor 字段**。
> 2. cap 算法是不是 `baseArmorMax + perm + temp`？
> 3. add perm/temp 时我有没有调 `applySlotArmorBonusDelta` 让 armor 立即刷？
> 4. temp 过期 / perm 减少时我有没有调 `applySlotArmorBonusDelta`（delta<0）让 armor 被 clamp 到新 cap？
> 5. 显示是不是单一数字 Z（不是 `X+Y`）？

任意一条不确定，**回去读 `equipment.ts` 顶部注释 + `combat.ts reduceResolveBlock` 再开口**。
